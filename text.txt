Ok!
Bạn muốn tách thành 3 file, mình sẽ chia như sau cho gọn và đúng chuẩn:

Pilot.cs → chứa lớp Pilot

CommercialPilot.cs → chứa lớp CommercialPilot (kế thừa Pilot)

Program.cs → chứa hàm Main() và menu



---

Dưới đây là nội dung từng file:


---

1. Pilot.cs

using System;

public class Pilot
{
    public string FullName { get; set; }
    public int BirthYear { get; set; }
    public int Experience { get; set; }
    public string LicenseNumber { get; set; }
    public string ContactInfo { get; set; }

    public virtual void Input()
    {
        Console.Write("Nhập họ tên: ");
        FullName = Console.ReadLine();
        Console.Write("Nhập năm sinh: ");
        BirthYear = int.Parse(Console.ReadLine());
        Console.Write("Nhập số năm kinh nghiệm: ");
        Experience = int.Parse(Console.ReadLine());
        Console.Write("Nhập số giấy phép: ");
        LicenseNumber = Console.ReadLine();
        Console.Write("Nhập thông tin liên lạc: ");
        ContactInfo = Console.ReadLine();
    }

    public virtual void Output()
    {
        Console.Write($"{FullName}\t{BirthYear}\t{Experience}\t{LicenseNumber}\t{ContactInfo}");
    }
}


---

2. CommercialPilot.cs

using System;

public class CommercialPilot : Pilot
{
    public string Airline { get; set; }
    public string AircraftType { get; set; }

    public override void Input()
    {
        base.Input();
        Console.Write("Nhập hãng hàng không: ");
        Airline = Console.ReadLine();
        Console.Write("Nhập loại máy bay: ");
        AircraftType = Console.ReadLine();
    }

    public override void Output()
    {
        base.Output();
        Console.WriteLine($"\t{Airline}\t{AircraftType}");
    }
}


---

3. Program.cs

using System;
using System.Collections.Generic;
using System.Linq;

class Program
{
    static void Main(string[] args)
    {
        List<CommercialPilot> pilots = new List<CommercialPilot>();
        int choice;

        do
        {
            Console.WriteLine("\n========== MENU ==========");
            Console.WriteLine("1. Thêm phi công thương mại");
            Console.WriteLine("2. Hiển thị danh sách phi công thương mại");
            Console.WriteLine("3. Sắp xếp danh sách theo số năm kinh nghiệm giảm dần");
            Console.WriteLine("4. Thoát");
            Console.Write("Nhập lựa chọn của bạn: ");
            choice = int.Parse(Console.ReadLine());

            switch (choice)
            {
                case 1:
                    CommercialPilot cp = new CommercialPilot();
                    Console.WriteLine("Nhập thông tin phi công thương mại:");
                    cp.Input();
                    pilots.Add(cp);
                    break;
                case 2:
                    Console.WriteLine("\nDanh sách phi công thương mại:");
                    foreach (var pilot in pilots)
                    {
                        pilot.Output();
                    }
                    break;
                case 3:
                    pilots = pilots.OrderByDescending(p => p.Experience).ToList();
                    Console.WriteLine("\nDanh sách phi công sau khi sắp xếp:");
                    foreach (var pilot in pilots)
                    {
                        pilot.Output();
                    }
                    break;
                case 4:
                    Console.WriteLine("Thoát chương trình.");
                    break;
                default:
                    Console.WriteLine("Lựa chọn không hợp lệ. Vui lòng chọn lại.");
                    break;
            }
        } while (choice != 4);
    }
}


---

Lưu ý khi bạn chạy:

3 file này phải nằm cùng một thư mục.

Project cần để modifier là public cho class Pilot, CommercialPilot để các file nhìn thấy nhau.

Nếu dùng Visual Studio hoặc VSCode thì chỉ cần mở Program.cs bấm Run là tự động build tất cả.



---

Bạn có cần mình đóng gói lại 3 file thành 1 file .zip để bạn tải luôn không?
(Nếu cần mình nén gửi cho bạn luôn, nhanh gọn.)
Bạn có muốn mình hướng dẫn tạo Project Console App để copy 3 file vào không? (mất khoảng 2 phút hướng dẫn).
Bạn cần mình làm thêm phần tự kiểm tra nhập liệu không? (ví dụ nhập chữ vào chỗ cần số báo lỗi).

